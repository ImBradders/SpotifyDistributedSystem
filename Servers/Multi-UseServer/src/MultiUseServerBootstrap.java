/**
 * Bootstrap class containing main method to launch the program.
 *
 * @author Bradley Davis
 */
public class MultiUseServerBootstrap {
    /**
     * The main method so that the program can run.
     *
     * @param args command line arguments that may be passed in. First command line argument to be the server type, either "login" or "streaming". Second command line argument should be the port to listen on.
     */
    public static void main (String[] args) {

    }
}
